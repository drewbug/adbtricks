<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.NFC" />

    <application>
        <provider
            android:name=".ShellContentProvider"
            android:authorities="adbtricks"
            android:exported="true" />
    </application>

</manifest>
